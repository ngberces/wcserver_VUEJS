// Name: Berces Neil
// App Name: conditionalif.js (Figure 14. The  Condition script coded )
// Description: conditionalif.js that will interpolate the message from the html parameters.

const ConditionalRendering = {
    //Create functions, data
    data(){
        return {
            seen: true
        }
    }
}

Vue.createApp(ConditionalRendering).mount('#conditional-rendering')